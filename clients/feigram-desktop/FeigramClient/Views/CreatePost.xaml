<Page x:Class="FeigramClient.Views.CreatePost"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FeigramClient.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CreatePost"
      Background="#F2F2F2">


    <Border CornerRadius="15" Padding="20" Background="White" Margin="180">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Botón de cerrar -->
            <Button HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Cancel_Click">
                <Image Source="/Resources/close.png" Width="27" Height="27"/>
            </Button>

            <!-- Contenido -->
            <StackPanel Grid.Row="1" Margin="40" VerticalAlignment="Top" HorizontalAlignment="Center" Width="500">

                <!-- Selector de imagen -->
                <Button Click="SelectImage_Click"
                        Background="#E0E0E0"
                        BorderBrush="Transparent"
                        Padding="10"
                        Cursor="Hand"
                        HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Resources/add.png" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Seleccionar imagen" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Button>

                <!-- Preview de imagen -->
                <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="10" Padding="10" Background="#FAFAFA" Visibility="{Binding ImageVisibility}">
                    <Grid>
                        <Image x:Name="PreviewImage" Stretch="Uniform" Height="300"/>
                        <Button Content="✖"
                                Width="30"
                                Height="30"
                                Background="#FF5252"
                                Foreground="White"
                                FontWeight="Bold"
                                BorderBrush="Transparent"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Margin="5"
                                Click="RemoveImage_Click"
                                Cursor="Hand"/>
                    </Grid>
                </Border>

                <!-- Descripción -->
                <TextBox x:Name="DescriptionBox"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="100"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         BorderBrush="#CCC"
                         Padding="10"/>

                <!-- Botones Publicar y Cancelar -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar"
                            Click="Cancel_Click"
                            Padding="15,5"
                            Background="#E0E0E0"
                            BorderBrush="Transparent"
                            Cursor="Hand"/>
                    <Button Content="Publicar"
                            Click="Publish_Click"
                            Padding="15,5"
                            Background="#2196F3"
                            Foreground="White"
                            BorderBrush="Transparent"
                            Cursor="Hand"/>
                </StackPanel>

            </StackPanel>
        </Grid>
    </Border>
</Page>
